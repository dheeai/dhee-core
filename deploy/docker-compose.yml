services:
  dhee-core-prod:
    image: ${PROD_IMAGE:-dhee-core:latest}
    container_name: dhee-core-prod
    restart: unless-stopped
    env_file:
      - .env.prod
    environment:
      PORT: 8080
      NODE_ENV: production
    expose:
      - "8080"

  dhee-core-dev:
    image: ${DEV_IMAGE:-dhee-core:latest}
    container_name: dhee-core-dev
    restart: unless-stopped
    env_file:
      - .env.dev
    environment:
      PORT: 8080
      NODE_ENV: production
    expose:
      - "8080"

  nginx:
    image: nginx:1.27-alpine
    container_name: dhee-core-nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "81:81"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
