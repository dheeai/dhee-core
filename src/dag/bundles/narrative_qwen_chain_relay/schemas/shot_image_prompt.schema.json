{
  "type": "object",
  "required": ["chosenBaseShotNumber", "chosenBaseReason", "view", "elevation", "distance", "deltaText"],
  "properties": {
    "chosenBaseShotNumber": {
      "type": ["integer", "null"],
      "description": "Of the previousN prior shots provided, pick the one whose framing best supports THIS shot's composition. Null if this is shot 1 (no prior) or no prior is suitable."
    },
    "chosenBaseReason": {
      "type": "string",
      "description": "One sentence: why this prior shot is the best chain base (e.g. 'shot 2 had both characters in medium two-shot, ideal for tightening to a CU on Ruby')."
    },
    "view": {
      "type": "string",
      "enum": [
        "front view",
        "front-right quarter view",
        "right side view",
        "back-right quarter view",
        "back view",
        "back-left quarter view",
        "left side view",
        "front-left quarter view"
      ],
      "description": "Camera azimuth. back-right quarter view = OTS over right shoulder."
    },
    "elevation": {
      "type": "string",
      "enum": [
        "low-angle shot",
        "eye-level shot",
        "elevated shot",
        "high-angle shot"
      ],
      "description": "Camera vertical angle. Default eye-level shot."
    },
    "distance": {
      "type": "string",
      "enum": [
        "close-up",
        "medium shot",
        "wide shot"
      ],
      "description": "Camera framing distance."
    },
    "deltaText": {
      "type": "string",
      "minLength": 30,
      "description": "Natural-language description of what CHANGES from the chosen prior shot to this one — new actions, character entries, expression changes, camera motion. Do not restate things that haven't changed. Do not add the <sks> prefix; the runner assembles it."
    }
  }
}
